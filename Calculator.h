#ifndef CALCULATOR_H
#define	CALCULATOR_H

double constructNumber(std::string equation, int &i);
double calculate(std::string number1, std::string number2, char operatr);
double calculator(std::string equation, std::string prevAnswer);

#endif